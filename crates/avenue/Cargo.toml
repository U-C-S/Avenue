[package]
name = "avenue"
version = "0.0.1-init"
edition = "2024"

[dependencies]
bevy = { version = "0.19", default-features = false, features = ["3d", "ui"] }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[profile.release]
codegen-units = 1
lto = "thin"
